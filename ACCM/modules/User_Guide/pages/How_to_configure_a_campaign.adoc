include::ROOT:conf/Configuration_properties.adoc[leveloffset=0]

= How to configure Campaigns

== Introduction

This section provides the steps required to manage the campaigns in the {app-short-name}.

== Create Campaign

=== Prerequisites
* Ensure that the software matrix to be used for this software campaign is configured. See xref:How_to_configure_a_software_matrix.adoc[Creating a software matrix] for more information.

=== Steps
To create a campaign using the {app-short-name} WebUI, you must execute the following steps:

. Click the "CAMPAIGNS" option on the menu bar displayed on top of the screen.
+
image::CAMPAIGNS_menu.png[align="center"]

. All the campaigns will be listed:
+
image::CAMPAIGNS_menuSection.png[align="center"]

. Click the *Add campaign* button.
+
image::CAMPAIGNSSection_newButton.png[align="center"]

. Enter the required data in the popup window.
+
image::CAMPAIGNSSection_createCAMPAIGNS.png[align="left"]

. Enter all required values:
+
- *Name:* It is the campaign name. Must be unique and have a maximum of 18 characters.
- *Schedule:* (optional, enabling the *Schedule* toggle)
** *Download Date:* Specific date and time for the software download to the devices. Must be in the future and before Update Date.
** *Update Date:* Specific date and time for the start of the campaign's execution. Must be in the future and after Download Date.
- *Software matrix:* It is the name of the software matrix to use. At least one matrix must exist and must be created previously on the "SOFTWARE MATRIX" section.
- *Clusters*: At least one cluster must be selected from the suggested clusters. +
For each cluster you can select the target devices: +
* *All devices:* All cluster's devices
* *Exclude*: List of specific element names to exclude from the update within this cluster.
* *Included*: List of specific element names to include on the update within this cluster.

. Click the *SAVE* button.

== Edit Campaign

Editing is restricted once the campaign transitions to `Executing` or any terminal state to ensure orchestration consistency.

[NOTE]
====
Campaign details can only be edited when the campaign is in `Scheduled` or `Not Scheduled` state.
====

To edit a campaign using the {app-short-name} WebUI, you must execute the following steps:

. Click the "CAMPAIGNS" option on the menu bar displayed on top of the screen.
+
image::CAMPAIGNS_menu.png[align="center"]

. All the campaigns will be listed:
+
image::CAMPAIGNS_menuSection.png[align="center"]

. Locate the campaign to be modified and click the *Edit* button on the row.
+
image::CAMPAIGNSSection_editButton.png[align="center"]

. Modified the desired data in the popup window.
+
image::CAMPAIGNSSection_updateCAMPAIGN.png[align="left"]

. The values that can be updated are:
+
- *Software matrix:* It is the name of the software matrix to use. At least one matrix must exist and must be created previously on the "SOFTWARE MATRIX" section.
- *Schedule:* (optional, enabling the *Schedule* toggle)
** *Download Date:* Specific date and time for the software download to the devices. Must be in the future and before Update Date.
** *Update Date:* Specific date and time for the start of the campaign's execution. Must be in the future and after Download Date.
- *Software matrix:* It is the name of the software matrix to use. At least one matrix must exist and must be created previously on the "SOFTWARE MATRIX" section.
- *Clusters*: At least one cluster must be selected from the suggested clusters. +
For each cluster you can select the target devices: +
* *All devices:* All cluster's devices
* *Exclude*: List of specific element names to exclude from the update within this cluster.
* *Included*: List of specific element names to include on the update within this cluster.
. Click the *SAVE* button.

== Delete Campaign

Deleting a campaign completely removes it from the local database, scheduler, and corresponding Altiplano modules. Deletion is strictly blocked if the campaign is actively running.

[NOTE]
====
**Allowed Delete States:** `Scheduled`, `NotScheduled`, `Success`, `Canceled`, `Error`.
====

To delete a campaign using the {app-short-name} WebUI, you must execute the following steps:

. Click the "CAMPAIGNS" option on the menu bar displayed on top of the screen.
+
image::CAMPAIGNS_menu.png[align="center"]

. All the Campaigns will be listed:
+
image::CAMPAIGNS_menuSection.png[align="center"]

. Locate the campaign to be deleted and click the *Delete* icon in the specific row.
+
image::SOFTWAREMATRIX_deleteButton.png[align="center"]

include::ROOT:go-back-component.adoc[Back]